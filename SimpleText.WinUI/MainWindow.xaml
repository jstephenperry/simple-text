<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SimpleText.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Transparent root lets the Mica backdrop show through the chrome (Win11 look). -->
    <Grid x:Name="RootGrid"
          Background="Transparent"
          AllowDrop="True"
          DragOver="OnRootDragOver"
          Drop="OnRootDrop">

        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Key="F" Modifiers="Control" Invoked="OnShowFindInvoked"/>
            <KeyboardAccelerator Key="T" Modifiers="Control" Invoked="OnNewTabInvoked"/>
            <KeyboardAccelerator Key="F3" Invoked="OnFindNextInvoked"/>
            <KeyboardAccelerator Key="F3" Modifiers="Shift" Invoked="OnFindPreviousInvoked"/>
            <KeyboardAccelerator Key="Escape" Invoked="OnEscapeInvoked"/>
        </Grid.KeyboardAccelerators>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar: a draggable region (set via SetTitleBar) so the window's
             Mica material shows through the caption instead of a flat white bar. -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="36" Background="Transparent">
            <TextBlock x:Name="AppTitleText"
                       Text="SimpleText"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"
                       Style="{StaticResource CaptionTextBlockStyle}"/>
        </Grid>

        <MenuBar Grid.Row="1">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New" Click="OnNewClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="N" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSubItem x:Name="NewFromTemplateMenu" Text="New from Template"/>
                <MenuFlyoutItem Text="Open..." Click="OnOpenClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="O" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Save" Click="OnSaveClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Save As..." Click="OnSaveAsClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="S" Modifiers="Control,Shift"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Close Tab" Click="OnCloseTabClick">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="W" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Exit" Click="OnExitClick"/>
            </MenuBarItem>

            <MenuBarItem Title="Mode">
                <RadioMenuFlyoutItem x:Name="ModePlainTextItem" Text="Plain Text" GroupName="ModeGroup" IsChecked="True" Click="OnModePlainTextClick"/>
                <RadioMenuFlyoutItem x:Name="ModeMarkdownItem" Text="Markdown" GroupName="ModeGroup" Click="OnModeMarkdownClick"/>
                <RadioMenuFlyoutItem x:Name="ModeAsciiDocItem" Text="AsciiDoc" GroupName="ModeGroup" Click="OnModeAsciiDocClick"/>
                <RadioMenuFlyoutItem x:Name="ModeRstItem" Text="reStructuredText" GroupName="ModeGroup" Click="OnModeRstClick"/>
            </MenuBarItem>

            <MenuBarItem x:Name="InsertMenu" Title="Insert"/>

            <MenuBarItem Title="View">
                <MenuFlyoutSubItem Text="Theme">
                    <RadioMenuFlyoutItem x:Name="ThemeSystemItem" Text="System" GroupName="ThemeGroup" IsChecked="True" Click="OnThemeSystemClick"/>
                    <RadioMenuFlyoutItem x:Name="ThemeLightItem" Text="Light" GroupName="ThemeGroup" Click="OnThemeLightClick"/>
                    <RadioMenuFlyoutItem x:Name="ThemeDarkItem" Text="Dark" GroupName="ThemeGroup" Click="OnThemeDarkClick"/>
                </MenuFlyoutSubItem>
                <ToggleMenuFlyoutItem x:Name="WordWrapItem" Text="Word Wrap" Click="OnWordWrapClick"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem x:Name="ZoomInItem" Text="Zoom In" Click="OnZoomInClick" KeyboardAcceleratorTextOverride="Ctrl+Plus">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Add" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="ZoomOutItem" Text="Zoom Out" Click="OnZoomOutClick" KeyboardAcceleratorTextOverride="Ctrl+Minus">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Subtract" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Reset Zoom" Click="OnResetZoomClick" KeyboardAcceleratorTextOverride="Ctrl+0">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Number0" Modifiers="Control"/>
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>

            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="Open Templates Folder" Click="OnOpenTemplatesFolderClick"/>
            </MenuBarItem>
        </MenuBar>

        <InfoBar x:Name="SessionInfoBar"
                 Grid.Row="2"
                 IsOpen="False"
                 IsClosable="True"
                 Severity="Warning"/>

        <StackPanel x:Name="FindBar"
                    Grid.Row="3"
                    Orientation="Horizontal"
                    Spacing="8"
                    Padding="12,6"
                    Visibility="Collapsed">
            <TextBlock Text="Find:" VerticalAlignment="Center"/>
            <TextBox x:Name="FindTextBox"
                     Width="260"
                     AutomationProperties.Name="Find text"
                     KeyDown="OnFindTextBoxKeyDown"
                     TextChanged="OnFindTextChanged"/>
            <Button Content="Next" Click="OnFindNextClick" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Prev" Click="OnFindPreviousClick"/>
            <Button Content="X" Click="OnFindCloseClick" AutomationProperties.Name="Close find bar"/>
            <TextBlock x:Name="NoMatchesText"
                       Text="No matches"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                       Visibility="Collapsed"/>
        </StackPanel>

        <TabView x:Name="Tabs"
                 Grid.Row="4"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch"
                 CanReorderTabs="True"
                 IsAddTabButtonVisible="True"
                 AddTabButtonClick="OnAddTabButtonClick"
                 TabCloseRequested="OnTabCloseRequested"
                 SelectionChanged="OnTabSelectionChanged"
                 TabItemsChanged="OnTabItemsChanged"/>

        <Grid Grid.Row="5"
              Padding="12,4"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusLineCol" Grid.Column="0" Text="Ln 1, Col 1" FontSize="12" VerticalAlignment="Center"/>
            <TextBlock x:Name="StatusFileName" Grid.Column="1" Text="Untitled" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="StatusFileType" Grid.Column="2" Text="Plain Text" FontSize="12" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="UTF-8" FontSize="12" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
